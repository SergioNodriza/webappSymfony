{% extends 'baseTransition.html.twig' %}

{% block body %}

    <div class="centerText">

        <h1>{{ result.info }}</h1>

        <hr class="black">
        <br>

        <table class="Transaction">
            <tr>
                <th class="Transaction">{{ 'Transition'|trans }}</th>
                <th class="Transaction">{{ 'State'|trans }}</th>
            </tr>
            <tr>
                <td>{{ result.transition }}</td>
                <td>{{ result.state }}</td>
            </tr>
        </table>

    </div>

{% endblock %}